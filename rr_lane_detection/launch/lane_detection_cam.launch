<!--@file lane_detection_cam.launch
 -  @brief This launch file is used to run the lane detection node on each camera
 -  @author Matthew Post
 -  @author Toni Ogunmade(oluwatoni)
 -  @competition IARRC 2018
 -->
<launch>
  <arg name="camera" default=""/>
  <arg name="config_file" default=""/>

  <group ns="$(arg camera)">
    <node pkg="rr_lane_detection" type="lane_detection" name="lane_detection" output="screen"/>
  </group>
</launch>